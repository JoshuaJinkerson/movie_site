import React from 'react'

const Detail = (props) => {    
    return (
            <div>
                <img src={props.image} alt="" className="thumbnail"></img>
            </div>
        )
    }

export default Detail